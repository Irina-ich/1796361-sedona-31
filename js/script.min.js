const offer = document.querySelector(".offer"), modal = document.querySelector(".modal"), offerClose = modal.querySelector(".offer"), appointmentForm = modal.querySelector(".appointment-form"), first = modal.querySelector(".first"), second = modal.querySelector(".second"); offer.addEventListener("click", function (e) { e.preventDefault(), modal.classList.toggle("modal-show") }), offerClose.addEventListener("click", function (e) { e.preventDefault(), modal.classList.remove("modal-show") }), appointmentForm.addEventListener("submit", function (e) { first.value && second.value ? isStorageSupport && localStorage.setItem("first", first.value) : e.preventDefault() }), window.addEventListener("keydown", function (e) { 27 === e.keyCode && modal.classList.contains("modal-show") && (e.preventDefault(), modal.classList.remove("modal-show")) });
